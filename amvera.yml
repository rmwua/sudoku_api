meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: Dockerfile
  command: null
  args: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
